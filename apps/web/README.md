# Web

Next.js 15 + TypeScript web app for InChat.

## Scripts

```bash
corepack pnpm --filter @inchat/web dev
corepack pnpm --filter @inchat/web build
corepack pnpm --filter @inchat/web start
```

Routes are defined in `src/app`; reusable screen components live in `src/views`.
