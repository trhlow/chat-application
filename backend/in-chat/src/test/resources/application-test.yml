spring:
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/hlow_chat_test}

management:
  otlp:
    metrics:
      export:
        enabled: false
    tracing:
      export:
        enabled: false
