spring:
  mongodb:
    uri: ${SPRING_MONGODB_URI:${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/hlow_chat_test}}
    redis:
      repositories:
        enabled: false
  redis:
    enabled: false

management:
  otlp:
    metrics:
      export:
        enabled: false
    tracing:
      export:
        enabled: false

app:
  redis:
    enabled: false
