spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/in_chat}
    username: ${SPRING_DATASOURCE_USERNAME:inchat}
    password: ${SPRING_DATASOURCE_PASSWORD:inchat}
  jpa:
    show-sql: true
