spring:
  data:
    redis:
      repositories:
        enabled: false
  mongodb:
    uri: ${SPRING_MONGODB_URI:${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/hlow_chat_test}}

app:
  jwt:
    secret: test-jwt-secret-integration-test-32-chars-min
  redis:
    enabled: false

management:
  otlp:
    metrics:
      export:
        enabled: false
    tracing:
      export:
        enabled: false
