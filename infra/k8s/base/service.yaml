apiVersion: v1
kind: Service
metadata:
  name: inchat-api
  labels:
    app.kubernetes.io/name: inchat-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: inchat-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
